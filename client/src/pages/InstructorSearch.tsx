import PlaceholderPage from "../components/PlaceholderPage";

// Placeholder — real marketplace UI is a later phase. Backend routes
// under /api/instructors already exist.
export default function InstructorSearch() {
  return (
    <PlaceholderPage
      title="Find an Instructor"
      subtitle="Browse and book lessons with qualified instructors near you"
    />
  );
}
