import PlaceholderPage from "../components/PlaceholderPage";

// Placeholder — availability CRUD UI is a later phase. Backend routes
// under /api/availability already exist.
export default function InstructorAvailability() {
  return (
    <PlaceholderPage
      title="Manage Availability"
      subtitle="Add or remove time slots learners can book with you"
    />
  );
}
