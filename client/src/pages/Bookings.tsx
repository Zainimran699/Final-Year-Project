import PlaceholderPage from "../components/PlaceholderPage";

// Placeholder — real learner-side bookings list is a later phase. Backend
// routes under /api/bookings already exist.
export default function Bookings() {
  return (
    <PlaceholderPage
      title="My Bookings"
      subtitle="Your upcoming and past driving lessons"
    />
  );
}
