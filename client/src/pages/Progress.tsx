import PlaceholderPage from "../components/PlaceholderPage";

// Placeholder — real progress charts are a later phase. Backend routes
// under /api/progress already exist.
export default function Progress() {
  return (
    <PlaceholderPage
      title="My Progress"
      subtitle="Track your theory and hazard test scores over time"
    />
  );
}
