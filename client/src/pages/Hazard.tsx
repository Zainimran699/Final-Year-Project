import PlaceholderPage from "../components/PlaceholderPage";

// Placeholder — real hazard-perception UI is a later phase. Backend
// routes under /api/hazard already exist.
export default function Hazard() {
  return (
    <PlaceholderPage
      title="Hazard Perception"
      subtitle="Identify developing hazards from real driving footage"
    />
  );
}
