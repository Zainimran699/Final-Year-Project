import PlaceholderPage from "../components/PlaceholderPage";

// Placeholder — real theory-question CRUD is a later phase. Backend
// routes under /api/admin/theory (Phase 12) already exist.
export default function AdminQuestions() {
  return (
    <PlaceholderPage
      title="Theory Questions"
      subtitle="Create, edit, or remove theory-test questions"
    />
  );
}
