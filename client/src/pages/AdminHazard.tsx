import PlaceholderPage from "../components/PlaceholderPage";

// Placeholder — real hazard-question CRUD is a later phase. Backend
// routes under /api/admin/hazard (Phase 12) already exist.
export default function AdminHazard() {
  return (
    <PlaceholderPage
      title="Hazard Questions"
      subtitle="Create, edit, or remove hazard-perception scenarios"
    />
  );
}
